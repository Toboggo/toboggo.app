VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Fond de carte MapLibre — URL d'un style JSON (spec « MapLibre Style » v8).
# Fournisseur retenu au démarrage : OpenFreeMap (gratuit, sans clé, données OSM).
# Provider-agnostic : remplacer par un style MapTiler / Protomaps / self-host suffit.
# Vide ⇒ l'app parents retombe sur la carte stylisée hors-ligne (FakeMap),
# le back-office affiche « Carte indisponible ».
VITE_MAP_STYLE_URL=https://tiles.openfreemap.org/styles/liberty

# Géocodage MapTiler — recherche libre d'une ville/adresse/lieu (barre de
# recherche de l'app parents), indépendant du fond de carte ci-dessus. Sert
# uniquement à obtenir des coordonnées ; les parcs restent lus depuis la base
# Toboggo. Vide ⇒ la recherche de lieux est simplement désactivée.
VITE_MAPTILER_KEY=

# Product Analytics (PostHog) — app parents uniquement, voir
# apps/mobile/src/lib/analytics/ et docs/analytics/.
# Vide ⇒ analytics no-op complet : aucun appel à posthog.init(), aucune
# requête réseau PostHog. C'est l'état attendu en local, en CI, dans le
# Simulator et dans les sessions Claude Code — ne jamais renseigner ces
# variables pour ces usages.
# Ne JAMAIS mettre ici une clé de projet Production (ni aucune autre clé
# réelle) : ce fichier est suivi par git. La configuration Production est
# injectée uniquement via les variables d'environnement du déploiement
# (jamais commitée), à l'image de VITE_SUPABASE_URL/VITE_SUPABASE_ANON_KEY
# ci-dessus. Staging utilisera ultérieurement un projet PostHog distinct de
# la Production (non créé à ce stade) — voir docs/analytics/PRIVACY-RULES.md §7.
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=
