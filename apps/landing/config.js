// Fill these in with your real Supabase project values (Project Settings →
// API). The anon key is safe to expose client-side — it's what Supabase's
// Row Level Security policies are designed to be used with.
window.TOBOGGO_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "your-anon-key",
};
