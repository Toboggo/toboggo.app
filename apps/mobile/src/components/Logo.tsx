/**
 * The Toboggo mark + wordmark now live in the shared design system so the app,
 * back office and landing site stay in sync. Re-exported here to keep existing
 * import paths (`../../components/Logo`) working.
 */
export { Logo, LogoMark } from "@toboggo/design-system";
